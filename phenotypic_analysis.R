# Phenotypic analysis for rice breeding programme - 2026
# Author: Mathankumar
# Purpose: Simple summary of yield and other traits

# Load data (replace with your real file name later)
# data <- read.csv("phenotype_data_2026.csv")

# Example: Calculate mean yield by genotype
# mean_yield <- aggregate(Yield ~ Genotype, data = data, FUN = mean)

# Example: Selection index (simple weighted)
# selection_index <- (0.6 * Yield) + (0.4 * PlantHeight)

# Print summary
# print(mean_yield)

cat("Phenotypic analysis script ready.\n")
cat("Add your real data and calculations here.\n")
